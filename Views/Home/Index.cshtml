@model HomeViewModel

@{
    ViewData["Title"] = "Favorites";
    //Layout = "_Layout";
}

<h1>Favorites</h1>
<div class="row mt-5 mb-2">
    @{                        
        foreach(Laptop laptop in Model.favLaptops)
        {
                @Html.Partial("Laptops", laptop)
        }
    }
</div>